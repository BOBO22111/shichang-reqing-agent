# 市场热度情绪智能体 · 项目说明

**参赛标识：币安 Agent 参赛作品 · 方向：数据分析**

**项目仓库：shichang-reqing-agent**

---

## 一、一句话介绍

对全市场永续合约做一次横截面扫描，计算每个币种的**热度分数**、识别**热门与冷门赛道**、
找出**资金正在出逃的币种**、追踪**资金在大饼与小币之间的轮动方向**，
最后把整个市场的状态归入**狂热 / 活跃 / 中性 / 低迷 / 恐慌**五档情绪之一。

> 不预测涨跌，只回答一个问题：**今天这个市场，钱在往哪里去。**

---

> **Agent 形态与币安官方资源（必读）**
>
> 本作品除网页外，还提供一条命令行入口 `agent.mjs` 和一份按**币安官方技能规范**编写的技能定义
> （`skills/shichang-reqing/SKILL.md`，可用 `npx skills add` 一键装载）。
>
> 两条官方数据通道：`--skill` 直接调用**币安官方技能市场**里 `binance` 技能所驱动的 `binance-cli`
> 取全市场行情；`--official` 读取**币安官方开源的公开数据仓库**（合约 K 线 + 合约指标，**附带持仓量**），
> 用来复现已验证过的历史截面。全程不需要 API 密钥，不涉及任何交易操作。
>
> 一句话：**用官方技能包拿数据，用本作品自研的引擎出结论。**
> 用到哪些官方资源、哪些为什么没用，逐项写在《AGENT.md》里。

---

## 二、项目背景：热度不等于涨幅

同样是上涨，有的币是巨量资金推动，有的币只是几笔小单拉起来的。
只看涨跌幅，会把这两类完全不同的行情混为一谈。

真正决定「热度」的，至少有三个额外维度：

- **成交额** —— 有多少钱真的参与进来了；
- **换手强度** —— 这些钱是进来就躺着，还是在高频倒手；
- **波动幅度** —— 价格是一路推升，还是来回剧烈拉扯。

只看单一维度的排行，结论很容易失真：一个成交清淡的小币，靠几笔单子拉出 30% 涨幅，
就能排到涨幅榜第一，但它对市场整体热度没有任何意义。

所以本项目的做法是：**把四个维度分别换算成全市场百分位，再加权合成一个 0–100 的热度分数**，
让「热度」这个模糊的概念变成一个可比、可排序、可解释的数字。

第二个视角是**结构**。单个币热不热，说明不了市场整体怎么样。
真正有价值的是广度信息：全市场有多少币在涨？资金是普遍铺开还是往少数几个方向集中？
钱是在从中小币往大饼撤，还是从大饼往中小币铺？——这些是「市场情绪」的骨架。

第三个视角是**赛道**。资金从来不是均匀流动的，它总是先在某个叙事方向聚集。
识别出哪一个赛道正在变热、哪一个正在被抛弃，比看单个币的排行更有前瞻性。

---

## 三、核心功能

### 3.1 币种热度排行（双核心功能之一）

对全市场每个永续合约计算热度分数，四个分项的权重如下：

| 分项 | 权重 | 为什么是这个权重 |
|---|---|---|
| 24 小时涨跌幅 | 30% | 方向性最强，但单独看会失真 |
| 24 小时成交额 | 30% | 反映真实参与资金量 |
| 换手强度 | 20% | 反映资金周转速度（口径见第五节） |
| 波动幅度 | 20% | 反映价格是否被真实推动 |

**关键处理：成交额先取对数再排名。**
大饼的日成交额可能是小币的十万倍，直接排名会让几乎所有币挤在一起失去区分度，
所以先用 `log10(成交额 + 1)` 做量级压缩，再计算百分位。

**关键处理：换手强度排名前先做极值裁剪。**
个别币的换手强度可能极端异常（小众合约持仓量极小时会出现极大比值），
因此先把数值裁剪到全市场的 P5–P95 区间再排名，避免个别离群值把整个分布压扁。

**百分位排名的好处：** 结果只反映「这个币相对全市场的位置」，
不受当期行情绝对水平影响 —— 牛市中不会所有币都是 90 分，熊市中也不会所有币都是 10 分。

页面提供四种榜单：**最热 20、最冷 20、换手最高、全部**，并支持按币种代号或赛道名称搜索。
搜索范围是**全部合约**，不是当前榜单内部。

### 3.2 市场情绪五档（核心输出）

先计算四个全市场广度指标：

| 指标 | 含义 |
|---|---|
| 上涨家数占比 | 全市场有多少比例的合约在涨（涨跌幅 ±0.1% 以内算平盘） |
| 成交额加权涨跌幅 | 按成交额加权的平均涨跌，反映**主流资金**的方向 |
| 高热度币种占比 | 热度分数 ≥ 70 的币种占全市场的比例 |
| 大饼成交额占比 | 大饼成交额 ÷ 全市场成交额，反映资金是否在避险集中 |

再合成情绪指数（0–100）：

```text
上涨占比得分 = 上涨家数占比 × 100
加权涨跌得分 = 分段映射（≥+3% → 100，0 → 50，≤−3% → 0）
高热度得分   = 高热度币种占比 × 100

情绪指数 = 0.40 × 上涨占比得分 + 0.30 × 加权涨跌得分 + 0.30 × 高热度得分 − 避险扣分
避险扣分 = 大饼占比 ≥ 60% 扣 8 分；≥ 70% 再扣 6 分
```

分档阈值：

| 档位 | 情绪指数 |
|---|---|
| 狂热 | ≥ 78 |
| 活跃 | 65 – 78 |
| 中性 | 45 – 65 |
| 低迷 | 30 – 45 |
| 恐慌 | < 30 |

> 这四个阈值可以在 `heat-engine.js` 顶部的常量里直接调整，
> 所有阈值都集中在一处，改参数不需要翻代码。

**页面同时展示四个分量的原始数值**，所以任何一个档位结论都能被逐项复核，
不是只给一个黑盒分数。

### 3.3 赛道冷热识别

公开行情接口不提供「这个币属于哪个赛道」，所以项目内置了一张
**「币种 → 赛道」映射表**（`sectors.js`），覆盖 200 多个主流币种代码，分为 12 个赛道：

> 公链与基础设施、二层与扩容、DeFi 协议、预言机与中间件、MEME 文化、AI 与算力、
> 游戏与元宇宙、存储与数据、跨链与桥、支付与结算、隐私加固、老牌分叉币

映射表没有收录的币种统一归入**「其他」**，页面会**如实显示「其他」赛道占全市场成交额的比例**，
不做隐藏。

- **赛道热度** = 赛道内所有币种按**成交额加权的平均热度分数**
- **热门赛道** = 排名前 3 的赛道
- **冷门赛道** = 排名后 3 的赛道，且要求该赛道至少 3 个币种（样本太少不下结论）

页面底部还有一张**赛道热度矩阵**：每个方块是一个币种，颜色由热度分数决定
（冷蓝 → 中性灰 → 热红），按赛道分组排列，一眼就能看出钱堆在哪个方向。

### 3.4 资金出逃识别

同时满足以下**三条横截面条件**的币种才进入出逃列表：

```text
① 24 小时跌幅 ≤ −4.5%              （价格明确下跌）
② 成交额排名进入全市场前 40%        （是活跃资金，不是无人问津的僵尸合约）
③ 换手强度排名进入全市场前 40%      （资金在快速倒手，说明在离场而不是沉淀）
```

第四条是**会话内增强条件**（需要至少两次刷新才有基线）：

```text
④ 持仓量较上一次快照下降 ≥ 3%        （真金白银在撤出合约）
```

**为什么第四条要单独处理：** 公开行情接口只提供当前值，不提供历史序列。
所以项目把每次快照记在**浏览器本地存储**里，下次刷新时就能算出变化。
首次打开页面时该条件不可用，页面会明确写「持仓量变化等待下一次刷新」，
**不会用任何方式猜测或填补**。

出逃强度分数由跌幅与换手强度两个百分位各占一半合成，用于列表排序。

### 3.5 资金轮动监测（双核心功能之二）

核心问题是：**钱是在从中小币往大饼撤，还是从大饼往中小币铺？**

计算方式：

```text
小币组   = 按成交额排序的第 21 – 100 名（刻意避开头部大币，避免大饼自身把结论带偏）
小币表现 = 小币组的成交额加权平均涨跌幅
大饼表现 = 大饼的 24 小时涨跌幅
超额表现 = 大饼表现 − 小币表现
```

判定规则：

| 条件 | 结论 |
|---|---|
| 超额表现 > +1.5 个百分点 | **资金回流大饼**（小币 → 大饼） |
| 超额表现 < −1.5 个百分点 | **资金轮动到小币**（大饼 → 小币） |
| 其余 | **轮动平稳 · 方向不明** |

**双重确认：** 除了超额表现，页面还计算「大饼成交额占比」的会话内变化方向。
如果两个信号一致，结论标注「大饼占比变化已确认」；不一致则标注「尚未确认」。
这样用户能区分「单一信号」和「双重确认」的结论强度。

界面用一条**双向流向带**呈现：左端是大饼、右端是小币，中间指针的位置由超额表现决定，
指针偏向哪边就是钱在往哪边流。

---

## 四、与「恐慌贪婪指数」的区别（答辩重点）

市面上常见的恐慌贪婪指数是**单一资产视角** —— 主要基于大饼自身的波动率、市场动量、
社交媒体讨论量和搜索热度，再由问卷数据加权得出。它回答的是「**大饼这一个币**的情绪怎么样」。

本项目是**全市场横截面视角**，两者的区别是结构性的：

| 对比项 | 常见恐慌贪婪指数 | 本项目 |
|---|---|---|
| 观察对象 | 主要是大饼单一资产 | 全市场 300 多个永续合约 |
| 数据来源 | 含社交媒体、搜索、问卷 | **只用可验证的行情数据** |
| 核心方法 | 单一资产的动量与波动率打分 | 全市场广度结构 + 资金轮动方向 |
| 能否定位到具体币 | 不能 | 能，给出完整的币种热度排行 |
| 能否定位到赛道 | 不能 | 能，给出赛道冷热排行 |
| 更新方式 | 低频（通常按天） | 页面自动刷新，可手动随时刷新 |
| 可复现性 | 部分依赖第三方问卷数据 | **同样输入必然得到同样输出** |

一句话概括：**它看一个币，我们看一整张网。**

---

## 五、数据来源与计算口径

### 5.1 数据来源

| 项目 | 说明 |
|---|---|
| 主数据源 | **各大平台公开行情接口**（USDT 永续合约口径） |
| 备用数据源 | **各大平台公开行情接口**（链上永续合约口径） |
| Agent 形态的官方通道 | **币安官方开源公开数据仓库**（合约 K 线 + 合约指标，带持仓量，用于复现历史截面）与**币安官方公开行情接口**；以及**币安官方技能市场** `binance` 技能驱动的 `binance-cli`（`--skill` 通道）。详见《AGENT.md》 |
| 覆盖范围 | 主数据源一次返回全部 USDT 永续合约；备用源覆盖链上永续市场；官方通道覆盖成交额靠前的 N 个合约（默认 120，可用 `--top` 调整） |
| 是否需要密钥 | **不需要**，全部是公开行情与公开数据入口 |
| 是否涉及交易 | **不涉及**。不接入交易权限，不保存私钥，不执行任何下单 |
| 是否涉及账户 | **不涉及**。不登录、不绑定、不读取任何账户信息 |

> **不同通道的口径不同**（官方合约 / 官方现货 / 多平台合约），数字不能直接对比。
> Agent 命令行的输出里会标明当前使用的通道与口径。

### 5.2 关于「换手率」的诚实说明（重要）

需求里提到要抓「换手率」。标准口径的换手率是：

```text
换手率 = 24 小时成交额 ÷ 流通市值
```

但经过逐字段核对，**两个数据源都不返回流通量、流通市值或总供应量**，
所以**标准口径的换手率算不出来**。

本项目的处理方式是改用**换手强度**：

```text
换手强度 = 24 小时成交额 ÷ 持仓量美元值
```

含义是「成交额是持仓量的多少倍」，衡量的是合约资金周转的快慢。
数值越大，说明同样规模的持仓被反复交易的次数越多，资金越活跃。

**这个替代口径在页面上被明确标注**，不会伪装成换手率。

### 5.3 各字段的换算口径

| 字段 | 换算方式 |
|---|---|
| 持仓量美元值 | 张数 × 每张乘数 × 标记价（主源）／ 持仓量币数 × 标记价（备用源） |
| 换手强度 | 24 小时成交额 ÷ 持仓量美元值 |
| 波动幅度 | （24 小时最高价 − 最低价）÷ 最低价 |
| 年化资金费率 | 主源按 8 小时结算：× 3 × 365；备用源按 1 小时结算：× 24 × 365 |
| 涨跌幅（备用源） | 备用源不直接提供，用「标记价 ÷ 前日参考价 − 1」计算 |

**波动幅度的降级处理：** 只有主数据源提供 24 小时最高价与最低价。
当切换到备用源时，该列显示「—（该数据源不提供）」，
**不做估算、不用涨跌幅反推、不留假数**。

### 5.4 统计筛选口径

- **只统计 USDT 计价的永续合约**
- **排除稳定币互兑对**（例如 USDC/USDT 这类，其涨跌没有分析意义）
- **剔除 24 小时成交额低于 20 万美元的僵尸合约**，避免长期无成交的合约污染市场广度统计

以上三条筛选都会影响「全市场」的口径，因此在本文件与页面上都明确写出，
页面上同时显示实际参与统计的合约数量。

### 5.5 数据真实性原则

页面只展示接口真实返回的数据。当接口失败或超时时，
页面显示明确的中文失败原因，**不会用演示数据或固定数值顶替，也不会把演示数据描述成实时数据**。

---

## 六、技术实现

### 6.1 整体架构

采用**静态前端 + 双通道取数**架构：

```text
                    浏览器页面（仓库根目录）
                          │
                ┌─────────┴─────────┐
                │  通道一（优先）    │  通道二（自动兜底）
                ▼                   ▼
          /api/heat、/api/market   直连各大平台公开接口
          Netlify Functions              │
                │                        │
                └────────────┬───────────┘
                             ▼
              公开行情接口（综合平台 / 链上平台）
```

**通道一（云端接口代理）**：页面统一请求 `/api/xxx`，由 Netlify Functions 转发到上游。
优点是带服务端短时缓存（全市场快照 30 秒、K 线 6 秒）、统一处理超时与错误、
后续更换数据源只需改函数不必动页面。

**通道二（浏览器直连兜底）**：当站点没有部署云端函数时（例如把静态文件夹直接拖到
Netlify Drop），页面会自动检测到这一点，改用浏览器直接访问公开接口。
这样同一套页面代码既能跑在带函数的部署环境，也能跑在纯静态环境，**不会白屏**。

**通道自动切换的判定依据**：云端接口返回 404、返回的不是 JSON、或缺少成功标记时，
即判定云端不可用并切换到直连。页面顶部会**如实显示当前实际使用的通道**。

### 6.2 性能设计

全市场有 300 多个合约。如果对每个币种都请求一次 K 线，会产生几百个请求，页面必然卡死。
因此本项目的数据流是：

- **一次请求拿到全市场快照**（含价格、涨跌、成交额、持仓量、资金费率），
  所有排行榜、赛道、情绪、轮动结论都从这一份快照算出，**不需要任何额外请求**
- **K 线只在用户点开某个币种详情时才按需请求一次**
- 前端自动刷新间隔 60 秒，页面切到后台时自动暂停，回到前台且数据过期时补一次
- 服务端缓存：全市场快照 30 秒、K 线 6 秒

### 6.3 技术选型说明

| 部分 | 实现方式 | 原因 |
|---|---|---|
| 热度算法 | 纯 JavaScript 前端计算 | 全部基于接口返回的真实数据，不使用任何预置数值 |
| 气泡图 | 原生 Canvas 手绘 | 不加载任何外部图表库，避免 CDN 加载失败导致页面空白 |
| 迷你 K 线图 | 原生 Canvas 手绘 | 同上 |
| 赛道矩阵 | 纯 CSS 网格 + 动态颜色 | 300 多个方块仍能快速渲染，且无需图表库 |
| 取数通道 | 云端代理优先、浏览器直连兜底 | 兼顾服务端缓存与部署自由度 |
| 取数口径 | 两条通道口径完全一致 | 有专门的对照测试保证两条通道结果一字不差 |
| 本地调试 | 自建 dev-server | 复用与线上完全相同的函数代码，保证本地验证结果与线上一致 |

### 6.4 开发过程中发现并修复的真实缺陷

以下问题都是在**真实行情数据 + 真实浏览器环境**的自测中发现的，不是看代码看出来的：

| 缺陷 | 现象 | 修复方式 |
|---|---|---|
| 合约代码字段取错变量 | 浏览器通道算出的合约代码是空的，导致「点开币种详情看 K 线」直接失效 | 改为取原始合约代码字段，并新增「两条通道逐字段对照」测试 |
| K 线时间戳精度不统一 | 一个数据源给秒级时间戳、另一个给毫秒级，时间轴会错位 | 统一按位数判断并换算成毫秒 |
| 搜索范围被榜单截断 | 在大饼不在「最热 20」里时，搜索 BTC 返回空结果，看起来像功能坏了 | 改为先搜索全部合约、再按模式取子集，并在标题下方显示命中数量 |
| 数字与列表条数不一致 | 出逃面板顶部显示 13 个、列表却只画出 12 个，看起来像 bug | 统一规则：顶部显示总数，列表最多显示 20 个，超出时明确说明 |
| 画不出图时谎报「没有数据」 | 浏览器无法绘制图表时，提示写成「K 线数据为空」，与事实不符 | 区分「数据为空」与「环境画不出图」两种情况，分别给出准确说明 |
| 资源引用用了相对路径 | 页面被深路径访问时，脚本路径会解析错误 | 全部改为以 `/` 开头的绝对路径 |
| 搜索状态提示不清 | 用户不知道搜索是在全部合约里找还是在当前榜单里找 | 标题下方实时显示命中数量与搜索范围 |
| 官方仓库「面值币」代码（命令行入口） | 官方文件里 PEPE 的合约文件其实叫 `1000PEPEUSDT`，按 `PEPEUSDT` 直接取会整批 404，小币全部缺失 | 加候选名自动回退（1000 / 10000 / 1000000 前缀），命中的币数从 112 提到 114，并在数据质量区说明 |
| 官方公开行情入口的口径被标错（命令行入口） | 该入口只提供现货，输出里却写着「USDT 永续合约」 | 自动切换到现货口径，并在提醒里说明「现货没有持仓量，权重按比例分摊」 |
| 币种名单来源静默降级（命令行入口） | 名单来源接口的地址常量没有导出，导致名单悄悄退回内置清单，覆盖面变小且用户不知情 | 改用项目内同一地址常量，并在输出里打印名单来源与可用币数 |

---

## 七、页面结构

采用**顶部总览条 + 左右不等宽两栏 + 底部全宽矩阵**的仪表盘布局，共 9 个区块：

1. **顶部总览条（粘性）** —— 参赛标识、作品名、五档情绪徽章、情绪指数、
   取数通道、更新时间、刷新按钮、自动刷新开关
2. **总览指标带** —— 全市场成交额、全市场持仓量、上涨家数占比、成交额加权涨跌、
   涨跌中位数、大饼成交额占比
3. **全市场热度分布（气泡图）** —— 横轴涨跌幅、纵轴换手强度、气泡大小成交额、颜色涨跌方向，
   支持「全部 / 热度前 40 / 出逃嫌疑」三种筛选，鼠标悬停看细节，点击进入详情
4. **币种热度排行榜** —— 四种榜单切换 + 搜索，点击任意一行打开详情抽屉
5. **资金轮动监测** —— 轮动结论、双向流向带、超额表现、大饼占比及其变化、小币组范围
6. **资金出逃警示** —— 命中币种列表，每条写明跌幅、成交额、换手强度、持仓变化与出逃强度
7. **赛道冷热排行** —— 热门赛道 3 个、冷门赛道 3 个，含加权热度与币种数量
8. **赛道热度矩阵（全宽）** —— 按赛道分组的方块热力图
9. **数据来源与计算口径 / 风险提示** —— 共 11 条口径说明（含「Agent 形态与官方资源」一条）

另有**币种详情抽屉**（点击气泡、排行榜或矩阵方块时从右侧滑出）：
关键数字 8 项、热度分数四项构成条、近 48 根 1 小时 K 线迷你图、数据说明。

**视觉风格：** 浅灰蓝科技风（浅灰蓝底 `#eef2f8` + 白色卡片 + 靛蓝 `#4f46e5` 与青色 `#0ea5e9` 强调）。
按中国习惯，**上涨与做多用红色，下跌与做空用绿色**；情绪五档由热到冷依次为
红 → 橙 → 灰 → 青 → 绿。

**明确不做交易机器人的视觉暗示**：不出现下单按钮、不出现伪造订单、不出现「一键跟单」类表达。

---

## 八、使用方法

### 8.1 在线使用

打开部署地址后，页面会**自动加载**全市场快照并完成所有计算，无需任何配置。
默认每 60 秒自动刷新一次，也可以随时点「立即刷新」。

可以做的事：

- 看顶部五档情绪与情绪指数，判断今天市场的整体状态
- 看气泡图，一眼分辨「上涨且换手活跃」和「上涨但资金清淡」的币
- 切换排行榜的四种榜单，或用搜索框定位某个币种
- 看资金轮动结论，判断钱是往大饼走还是往小币走
- 看赛道冷热与底部矩阵，找到资金正在聚集的方向
- 点击任意币种，查看它的关键数字、热度分数构成与 K 线

### 8.2 本地运行

需要先安装 Node.js（18 或更高版本）。

```bash
cd "D:/币安波2作品"
node dev-server.mjs
```

启动后在浏览器打开：

```text
http://127.0.0.1:8791
```

本地服务器会复用与线上完全相同的函数代码，因此本地看到的结果与线上一致。

### 8.3 命令行使用（Agent 形态）

除了网页，同一套引擎还提供一条命令行入口，供 AI Agent 或脚本直接调用：

```bash
cd "D:/币安波2作品"

node agent.mjs                       # 多平台实时口径（与网页版完全一致）
node agent.mjs --official            # 币安官方公开数据（合约口径 + 持仓量，可复现）
node agent.mjs --official --top 200  # 分析成交额前 200 个合约
node agent.mjs --live                # 币安官方公开行情接口（全市场现货快照）
node agent.mjs --skill               # 币安官方技能通道（未装 binance-cli 会如实回退；主站入口不可达时用官方公开行情入口）
node agent.mjs --official --json     # 输出 JSON，便于程序解析
```

命令行用的是与网页**完全相同**的 `heat-engine.js`，所以两边结论一致。
用到哪些官方资源、哪些为什么没用，见《AGENT.md》；技能用法见 `skills/shichang-reqing/SKILL.md`。

---

## 九、目录结构

```text
币安波2作品/
├─ index.html                   # 页面骨架
├─ styles.css                   # 浅灰蓝主题与仪表盘布局
├─ favicon.svg
├─ sectors.js                   # 内置「币种 → 赛道」映射表
├─ market-snapshot.js           # 快照归一化与数值格式化
├─ heat-engine.js               # 热度分数 / 情绪五档 / 赛道聚合 / 出逃 / 轮动
├─ history-tracker.js           # 会话内快照留存与环比
├─ data-source.js               # 浏览器直连取数
├─ api-client.js                # 取数通道选择与自动兜底
├─ chart.js                     # 自绘气泡图与迷你 K 线图
├─ app.js                       # 页面渲染与交互
├─ agent.mjs                    # Agent 命令行入口（四条数据通道）
├─ AGENT.md                     # Agent 说明与官方资源逐项取舍表
├─ skills/
│  └─ shichang-reqing/
│     └─ SKILL.md               # 按币安官方技能规范编写的技能定义
├─ functions/                   # 云端接口代理（Netlify Functions）
│  ├─ _data.cjs                 # 两个数据源的抓取与统一换算
│  ├─ heat.cjs                  # /api/heat   全市场快照
│  └─ market.cjs                # /api/market 单币 K 线
├─ netlify.toml                 # 部署与路由配置
├─ package.json
├─ dev-server.mjs               # 本地测试服务器（端口 8791）
├─ 打包上传文件夹.mjs           # 生成两个可直接拖拽上传的文件夹
├─ 检查上传格式.mjs             # 上传前格式体检
├─ README.md                    # 技术文档
├─ 项目说明.md                  # 本文档
├─ 上传到GitHub-操作步骤.md
├─ 上传到Netlify-操作步骤.md
└─ 参赛回复-评委两问.md
```

**关于目录结构：** 网页文件全部直接位于仓库根目录，整个项目只有 `functions/`（云端接口）
与 `skills/shichang-reqing/`（按官方技能规范编写的技能定义）两个约定目录 ——
与仓库里的实际结构一致。

上传用的是打包脚本生成的 `D:\币安波2作品-GitHub上传`，里面按「文件最终要待的位置」
分成了 `1-传到仓库根目录` / `2-传到functions目录` / `3-传到skills目录` 三个编号文件夹。
这样做是因为 GitHub 网页版上传只接受单独的**文件**、不接受文件夹；按目标位置分好之后，
每一步只要 `Ctrl+A` 全选拖进去就行，既不用挑文件，也不会和仓库里已有的同名文件冲突。

打包脚本还会自动拉取仓库文件树、逐字节比对（用 git 的 blob 哈希），生成《本次要传什么.md》，
列出这一次真正有变动的文件 —— 多数情况下 20 多个文件里只有几个需要上传，其余的不必重传。
详细步骤见《上传到GitHub-操作步骤.md》。

---

## 十、项目亮点总结（评审视角）

1. **视角差异化** —— 主流做法是做「恐慌贪婪指数」（看单一资产的情绪），
   本项目做的是**全市场横截面结构**：广度、赛道、轮动。同一个「市场情绪」命题，
   给出了一个完全不同的观察角度。
2. **每个数字都能被复核** —— 情绪指数展示四个分量的原始值，
   热度分数展示四项百分位排名，出逃币种展示三条条件的实际数值。
   没有黑盒结论，评审可以逐项验算。
3. **口径诚实** —— 算不了换手率就直接说明原因并给出替代口径；
   备用数据源没有波动幅度就留空并标注；首次打开没有环比基线就提示等待下一次刷新。
   整个过程**没有一个数字是编的**。
4. **量级处理有讲究** —— 成交额取对数再排名、换手强度先裁剪再排名，
   这两个处理直接决定了排行榜是否可信，很多同类项目会忽略。
5. **工程性能可控** —— 全市场 300 多个合约只发 1 个请求，
   K 线按需单币请求，避免了「逐币拉数据」这种常见但会拖死页面的做法。
6. **零外部依赖** —— 图表自绘、算法自算、无 CDN，页面加载失败风险低。
7. **部署方式自由** —— 同一套代码既能配合云端函数部署（带服务端缓存），
   也能纯静态直接上线（自动切换浏览器直连），不会因为部署方式受限而白屏。
8. **真实环境验证** —— 185 项自动化测试全部通过，其中包括在真实浏览器环境下
   完整加载页面、检查每一块面板是否真的渲染出数据。开发中发现的 7 个真实缺陷都是这样抓出来的。

---

## 十一、风险提示

本页面仅用于数字资产市场风险观测与数据分析，**不构成投资建议**，不代表任何买卖承诺。
页面中的热度分数、情绪档位、赛道冷热与资金轮动结论，均为基于公开行情数据的程序化统计结果，
**不等于实际交易建议**。

需要特别说明的几点：

- **热度分数不是价格预测。** 它衡量的是当期资金参与程度，不代表未来涨跌。
- **情绪档位描述的是当前截面状态。** 五档之间可能快速切换，尤其在高波动时段。
- **资金出逃识别是横截面统计。** 它指出「跌幅大 + 成交活跃 + 换手高」的币种，
  这种形态也可能出现在急跌反弹前，不构成任何方向性判断。
- **资金轮动结论依赖两个指标的相对关系。** 当超额表现绝对值很小时，
  结论含义有限，页面会显示「轮动平稳 · 方向不明」。
- **数据可能存在延迟、缺失或接口波动。** 页面会在失败时如实报错，不会掩盖。

市场有风险，投资需谨慎。任何投资决策应结合个人风险承受能力、资金状况与投资目标独立判断，
必要时咨询持牌专业机构。过往表现不预示未来收益。
